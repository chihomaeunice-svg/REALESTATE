# Nyumba Yangu — frontend

React + Vite + TypeScript + Tailwind CSS v4. See the repo root [README](../README.md) for full setup instructions.

```bash
npm install
npm run dev      # proxies /api to http://localhost:8080
npm run build
```
